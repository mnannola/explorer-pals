Explorer Pals is a collaborate vacation planner built using the epic stack.

Commands

To setup locally
`npm run start`

To run locally
`npm run dev`

This starts your app in development mode, rebuilding assets on file changes.